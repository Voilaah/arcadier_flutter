import 'resource.dart';
import '../responses.dart';
import '../client.dart';

class MarketplaceResource extends Resource {
  MarketplaceResource(Client client, String host, String apiVersion) : super(client, host, apiVersion);
}
